/*
    configure build.gradle project file
 */

repositories {    
    jcenter()
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    implementation fileTree (dir: 'libs', include: '*.aar')
}